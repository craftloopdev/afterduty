App/build
App/Pods
App/output
App/App/public
DerivedData
xcuserdata

# Cordova plugins for Capacitor
capacitor-cordova-ios-plugins

# Generated Config files
App/App/capacitor.config.json
App/App/config.xml

# Firebase config — referenced by the Xcode project as a required bundle
# resource, but NEVER committed (real or fake). Xcode Cloud restores it from
# the GOOGLE_SERVICE_INFO_PLIST_BASE64 workflow secret (ci_scripts/
# ci_post_clone.sh); locally, inject your own copy.
App/App/GoogleService-Info.plist
